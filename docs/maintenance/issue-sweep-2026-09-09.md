# Issue investigation, 9 September 2026

The frozen scope is the newest 30 **open** issues at the initial authenticated GitHub inventory: #569 through #512 by creation time, excluding pull requests and already-closed issues. The snapshot contained 107 open issues. All 77 older issues were commented as legacy and closed as `not_planned`; a separate GitHub API audit verified state, reason and exact comment for all 77. Backlog closure does not assert a defect was fixed.

Implementation starts from `a03584f6` in the isolated `issue-sweep-20260909` worktree. The primary checkout contains unrelated staged work and is preserved.

The final authenticated GitHub audit verified all 107 original issues: 77 legacy reports are closed as `not_planned`, six cloud reports (#559, #554, #553, #552, #551, #514) are closed as `completed`, and 24 retained reports remain open. There were no unexpected state changes or new open issues. Local implementation is not published or merged into the primary checkout; reports needing historical upstream evidence remain open.

## Per-issue evidence

| Issue | Area | Investigation and boundary | Coverage |
| --- | --- | --- | --- |
| [#569](https://github.com/autohandai/code-cli/issues/569) | OpenRouter rejection | Upstream cause unproven. Added typed error preservation, HTTP 200 error-envelope rejection and plain-text fallback. Reports contain only a generic provider wrapper. | tests/providers/OpenRouterClient.test.ts |
| [#567](https://github.com/autohandai/code-cli/issues/567) | Native history integrity | Reproduced a matching four-message failure: context cropping deletes its pending assistant call. Preserve unfinished exchanges and group reused call IDs by occurrence. Original report tool histories are absent. | tests/conversationCrop.spec.ts; tests/tuistory/native-context-crop.tuistory.test.ts |
| [#566](https://github.com/autohandai/code-cli/issues/566) | OpenRouter rejection | Upstream cause unproven. Added typed error preservation, HTTP 200 error-envelope rejection and plain-text fallback. Reports contain only a generic provider wrapper. | tests/providers/OpenRouterClient.test.ts |
| [#565](https://github.com/autohandai/code-cli/issues/565) | Deferred completion | Reproduced completed-summary false positives and added targeted diagnostics. Supplied historical response excerpts are truncated and do not expose the triggering suffix. | tests/core/agent/ResponseCompletionClassifier.test.ts; tests/tuistory/response-completion.tuistory.test.ts |
| [#564](https://github.com/autohandai/code-cli/issues/564) | Windows System32 startup | System directories beneath Windows were not rejected before runtime filesystem work. Add case-insensitive drive-aware containment and reject before ACL checks. Preserve legitimate UNC projects. Native Windows terminal execution remains unverified on macOS. | tests/workspaceSafety.spec.ts; tests/tuistory/unsafe-workspace-startup.tuistory.test.ts |
| [#563](https://github.com/autohandai/code-cli/issues/563) | Windows System32 startup | System directories beneath Windows were not rejected before runtime filesystem work. Add case-insensitive drive-aware containment and reject before ACL checks. Preserve legitimate UNC projects. Native Windows terminal execution remains unverified on macOS. | tests/workspaceSafety.spec.ts; tests/tuistory/unsafe-workspace-startup.tuistory.test.ts |
| [#562](https://github.com/autohandai/code-cli/issues/562) | Deferred completion | Reproduced completed-summary false positives and added targeted diagnostics. Supplied historical response excerpts are truncated and do not expose the triggering suffix. | tests/core/agent/ResponseCompletionClassifier.test.ts; tests/tuistory/response-completion.tuistory.test.ts |
| [#561](https://github.com/autohandai/code-cli/issues/561) | OpenRouter rejection | Upstream cause unproven. Added typed error preservation, HTTP 200 error-envelope rejection and plain-text fallback. Reports contain only a generic provider wrapper. | tests/providers/OpenRouterClient.test.ts |
| [#560](https://github.com/autohandai/code-cli/issues/560) | Empty MLX response | Bounded recovery already exists and is verified; improve empty-body diagnostics and cover retry exhaustion. Reason the upstream server returned an empty body remains unknown. | tests/providers/MLXProvider.test.ts |
| [#559](https://github.com/autohandai/code-cli/issues/559) | Fantail schema rejection | Historical CLI sends root messages correctly. Server Dynamic Route shaping caused the documented oneOf signature. Existing inference fixes and current deployed configuration bypass the implicated route. One authenticated Fantail canary returned HTTP 200 with a nonempty completion, capped at 64 output tokens. | tests/providers/AutohandAIProvider.test.ts; tests/providers/LLMGatewayClient.spec.ts; cloud-server-audit-wrangler.log |
| [#558](https://github.com/autohandai/code-cli/issues/558) | MLX memory guard | Classify prefill memory guard as context_overflow, preserving plain-text diagnostics, so the runtime can compact instead of treating it as malformed input. | tests/providers/apiErrors.test.ts; tests/providers/MLXProvider.test.ts |
| [#557](https://github.com/autohandai/code-cli/issues/557) | Deferred completion | Reproduced completed-summary false positives and added targeted diagnostics. Supplied historical response excerpts are truncated and do not expose the triggering suffix. | tests/core/agent/ResponseCompletionClassifier.test.ts; tests/tuistory/response-completion.tuistory.test.ts |
| [#556](https://github.com/autohandai/code-cli/issues/556) | Command output memory | Proved unbounded subprocess stream accumulation and bounded retained stdout/stderr/PTY output with marked head/tail capture. Preserve finished output when the legacy composer returns at the terminal bottom. Original compiled stack lacks exact source mapping; no exact historical OOM claim. | tests/utils/commandOutputCapture.test.ts; tests/command.spec.ts; tests/ui/shellCommand.test.ts; tests/tuistory/resource-errors.tuistory.test.ts |
| [#555](https://github.com/autohandai/code-cli/issues/555) | Native history integrity | Reproduced a matching four-message failure: context cropping deletes its pending assistant call. Preserve unfinished exchanges and group reused call IDs by occurrence. Original report tool histories are absent. | tests/conversationCrop.spec.ts; tests/tuistory/native-context-crop.tuistory.test.ts |
| [#554](https://github.com/autohandai/code-cli/issues/554) | Fantail schema rejection | Historical CLI sends root messages correctly. Server Dynamic Route shaping caused the documented oneOf signature. Existing inference fixes and current deployed configuration bypass the implicated route. One authenticated Fantail canary returned HTTP 200 with a nonempty completion, capped at 64 output tokens. | tests/providers/AutohandAIProvider.test.ts; tests/providers/LLMGatewayClient.spec.ts; cloud-server-audit-wrangler.log |
| [#553](https://github.com/autohandai/code-cli/issues/553) | Fantail schema rejection | Historical CLI sends root messages correctly. Server Dynamic Route shaping caused the documented oneOf signature. Existing inference fixes and current deployed configuration bypass the implicated route. One authenticated Fantail canary returned HTTP 200 with a nonempty completion, capped at 64 output tokens. | tests/providers/AutohandAIProvider.test.ts; tests/providers/LLMGatewayClient.spec.ts; cloud-server-audit-wrangler.log |
| [#552](https://github.com/autohandai/code-cli/issues/552) | Fantail schema rejection | Historical CLI sends root messages correctly. Server Dynamic Route shaping caused the documented oneOf signature. Existing inference fixes and current deployed configuration bypass the implicated route. One authenticated Fantail canary returned HTTP 200 with a nonempty completion, capped at 64 output tokens. | tests/providers/AutohandAIProvider.test.ts; tests/providers/LLMGatewayClient.spec.ts; cloud-server-audit-wrangler.log |
| [#551](https://github.com/autohandai/code-cli/issues/551) | Fantail schema rejection | Historical CLI sends root messages correctly. Server Dynamic Route shaping caused the documented oneOf signature. Existing inference fixes and current deployed configuration bypass the implicated route. One authenticated Fantail canary returned HTTP 200 with a nonempty completion, capped at 64 output tokens. | tests/providers/AutohandAIProvider.test.ts; tests/providers/LLMGatewayClient.spec.ts; cloud-server-audit-wrangler.log |
| [#550](https://github.com/autohandai/code-cli/issues/550) | Native history integrity | Reproduced a matching four-message failure: context cropping deletes its pending assistant call. Preserve unfinished exchanges and group reused call IDs by occurrence. Original report tool histories are absent. | tests/conversationCrop.spec.ts; tests/tuistory/native-context-crop.tuistory.test.ts |
| [#549](https://github.com/autohandai/code-cli/issues/549) | OpenRouter rejection | Upstream cause unproven. Added typed error preservation, HTTP 200 error-envelope rejection and plain-text fallback. Reports contain only a generic provider wrapper. | tests/providers/OpenRouterClient.test.ts |
| [#548](https://github.com/autohandai/code-cli/issues/548) | Windows System32 startup | System directories beneath Windows were not rejected before runtime filesystem work. Add case-insensitive drive-aware containment and reject before ACL checks. Preserve legitimate UNC projects. Native Windows terminal execution remains unverified on macOS. | tests/workspaceSafety.spec.ts; tests/tuistory/unsafe-workspace-startup.tuistory.test.ts |
| [#547](https://github.com/autohandai/code-cli/issues/547) | Windows System32 startup | System directories beneath Windows were not rejected before runtime filesystem work. Add case-insensitive drive-aware containment and reject before ACL checks. Preserve legitimate UNC projects. Native Windows terminal execution remains unverified on macOS. | tests/workspaceSafety.spec.ts; tests/tuistory/unsafe-workspace-startup.tuistory.test.ts |
| [#546](https://github.com/autohandai/code-cli/issues/546) | Memory storage exhaustion | Reproduced ENOSPC on event-log lock creation during startup/context. Optional memory context falls back to readable raw entries for ENOSPC/EDQUOT; explicit writes and corruption still fail. | tests/memory/MemoryManager.eventLog.test.ts; tests/tuistory/resource-errors.tuistory.test.ts |
| [#545](https://github.com/autohandai/code-cli/issues/545) | xAI API key | Replayed HTTP 400 incorrect-key response. Classify as non-retryable authentication failure with xAI guidance. Account credential validity is outside the client repair. | tests/providers/XAIProvider.test.ts; tests/providers/apiErrors.test.ts |
| [#544](https://github.com/autohandai/code-cli/issues/544) | Retired NVIDIA model | Replayed HTTP 410 retirement. Stop unchanged retries, remove the deprecated endpoint from bundled NVIDIA choices, and direct the user to /model. The retired provider model cannot be restored by CLI code. | tests/providers/NVIDIAClient.test.ts; tests/providers/apiErrors.test.ts; tests/providers/modelCatalog.test.ts |
| [#543](https://github.com/autohandai/code-cli/issues/543) | Empty MLX response | Bounded recovery already exists and is verified; improve empty-body diagnostics and cover retry exhaustion. Reason the upstream server returned an empty body remains unknown. | tests/providers/MLXProvider.test.ts |
| [#542](https://github.com/autohandai/code-cli/issues/542) | Provider model isolation | Reproduced Moa leaking into OpenAI settings on provider/auth switch and old provider persisting on save. Preserve target-owned model and save selected provider before restart. Existing corrupted settings require selecting a valid model. | tests/core/agent/ProviderConfigManager.openai.test.ts; tests/tuistory/provider-switch.tuistory.test.ts |
| [#514](https://github.com/autohandai/code-cli/issues/514) | Fantail gateway 2005 | Existing CLI retry fix 65b4ccc9 and server route-name/fallback corrections predate current deployment. Deployed Dynamic Route bindings are absent. One authenticated Fantail canary returned HTTP 200 with a nonempty completion, capped at 64 output tokens. | tests/providers/LLMGatewayClient.spec.ts; cloud-server-audit-wrangler.log |
| [#513](https://github.com/autohandai/code-cli/issues/513) | OpenRouter rejection | Upstream cause unproven. Added typed error preservation, HTTP 200 error-envelope rejection and plain-text fallback. Reports contain only a generic provider wrapper. | tests/providers/OpenRouterClient.test.ts |
| [#512](https://github.com/autohandai/code-cli/issues/512) | OpenRouter rejection | Upstream cause unproven. Added typed error preservation, HTTP 200 error-envelope rejection and plain-text fallback. Reports contain only a generic provider wrapper. | tests/providers/OpenRouterClient.test.ts |

## Local commits

Each implementation commit contains its regression coverage. These commits are local to `issue-sweep-20260909`; aggregate proof must be made green before integration or publication.

| Commit | Change | Issues |
| --- | --- | --- |
| `328e5506` | Preserve native tool exchanges when cropping context | #567, #555, #550 |
| `37911c86` | Classify completed replies without treating quoted examples as promises | #565, #562, #557 |
| `f026ca99` | Reject unsafe Windows startup directories before filesystem work | #564, #563, #548, #547 |
| `0b37f269` | Keep model settings owned by the selected provider | #542 |
| `9fa41708` | Preserve provider rejection semantics and stop retrying retired models | #569, #566, #561, #560, #558, #549, #545, #544, #543, #513, #512 |
| `5838425a` | Bound command output and preserve readable memory under storage pressure | #556, #546 |

The six cloud closures verify previously deployed fixes and successful current minimal inference.

## Validation

Aggregate proof is **not green**. Focused regression suites, lint, type checking and builds pass, but aggregate runs have subprocess/test-worker failures. Timeout thresholds and clean-exit requirements were preserved; incorrect fixture expectations were corrected against the renderer contracts.

| Check | Result |
| --- | --- |
| Regression-first implementation | Added tests were observed failing before the corresponding repairs; focused suites and independent review passed. |
| Final lint and type checking | Passed in standard proof after the final source changes. |
| Second CI proof | 9,168 tests passed, 2 timed out, 36 skipped; 623 files passed, 2 failed, 2 skipped. Exited 1 after 801.97 seconds. |
| Standard local proof | 9,124 tests passed, 3 failed, 36 skipped; 620 files passed, 3 failed, 2 skipped. Two additional files could not start workers. Exited 1 after 868.30 seconds. |
| Standard-proof failure rerun | All 96 tests in the 5 affected files passed without source changes, using one CI worker. This does not replace aggregate proof. |
| Final focused terminal regressions | Resource scenarios passed 3/3, including Ink and legacy output, a second shell command and clean exit. Unchanged resume scenarios passed 10/10. |
| Final build and full Tuistory | Passed: all 34 files; 156 tests passed and 1 Windows-only test skipped. Exited 0 after 1,203.82 seconds. |

The second CI run timed out in the unchanged Bash completion test (`tests/completions/shellCompletion.spec.ts`) and imported Claude hook test (`tests/import/hooks.test.ts`). Standard local proof failed the unchanged grouped-diff case (`tests/core/agent/ReactLoopRunnerStatus.test.ts`), restricted Blueprint process (`tests/modes/rpc/blueprintProcess.integration.test.ts`), and lifecycle-hook execution (`tests/lifecycleHookAuthoring.spec.ts`). Workers did not start for `tests/mcpClientManager.spec.ts` and `tests/releaseNotes.test.ts`. The grouped-diff fixture invokes real repository Git snapshots while the changed classifier and rendering paths are disabled or mocked. A read-only host audit did not establish the exact cause of the process delays.

The first combined terminal run passed 9 tests with 1 Windows-only skip. The first full terminal run then passed 152 tests, failed 3 and skipped 1. Its failures exposed fixture mistakes: the large-output test expected the wrong Ink marker and closed a mock twice, and the shared exit helper sent a third Ctrl+C after graceful shutdown had begun. The added legacy-renderer variant then reproduced the composer overwriting final output at the terminal bottom. The final fixtures distinguish the two renderers and wait for clean shutdown. The composer now reserves initial rows with newlines so terminal scrolling preserves completed output.

An earlier CI proof passed 9,164 tests and failed 6 across 5 unchanged startup/binary files; all 23 tests in those files passed on isolated rerun without source changes. Logs for every aggregate and focused rerun are retained in the worktree.

New terminal scenarios cover native context cropping, completed replies, provider selection/restart, unsafe startup, injected disk exhaustion and a 2 MiB command output line. Windows System32 terminal execution is platform-gated; portable Windows path tests run on macOS. Passing mocked provider tests does not prove a historical upstream request succeeded.

## External evidence

[OpenRouter error documentation](https://openrouter.ai/docs/api_reference/errors-and-debugging) specifies canonical error types and provider errors embedded in HTTP 200 responses. Provider details are retained while canonical types govern recovery.

Cloudflare deployment metadata: deployment `8e1f54b1-c34e-453b-89c6-a12c7ec24321`, version `d7663d35-4720-4057-871b-cfbceee28f89` at 100%, tag `monthly-cd35d53`. Current Fantail binding is `@cf/qwen/qwen3.8-27b`; `CODING_DYNAMIC_ROUTE`, `FANTAIL_DYNAMIC_ROUTE`, and `AUTO_DYNAMIC_ROUTE` are absent. A single authenticated Fantail request through api.autohand.ai returned HTTP 200 and a nonempty completion with a 64-token output cap. This proves current minimal inference as well as routing configuration, not replay of historical sessions.

Local audit artifacts are under `.autohand/issue-sweep-20260909/` in the primary checkout. They contain the frozen scope and independent legacy-closure verification.

NVIDIA confirms the [Mistral Small 4 hosted endpoint is deprecated](https://build.nvidia.com/mistralai/mistral-small-4-119b-2603/playground). Manual model IDs and custom inference endpoints remain available.

Two additional issues (#570 and #571) were synthetic reports accidentally emitted by the injected disk-full test. Their fixture stacks were verified and both were closed as not planned with explanatory comments. Shared Tuistory configuration now disables application auto-reporting and process-level automatic reporting. They are excluded from the original 30-issue inventory.
